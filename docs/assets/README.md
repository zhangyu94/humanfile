This directory now just groups subdirectories for each demo:

- `cli-demo/` — CLI-focused terminal demo (`demo.*`).
- `action-demo/` — GitHub Action / CI-focused demo (`action-demo.*`).

See the `README.md` files in each subdirectory for details on how to regenerate GIF/MP4 assets and what each cassette records.
