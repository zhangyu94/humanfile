# Assets

- `cli-demo/` — Classification / provenance (`check`, `ls`, `explain`); `cli-demo.*`.
- `install-demo/` — `humanfile install` walkthrough for README Quick Start step 2; `install-demo.*`.
- `guard-demo/` — `humanfile guard` walkthrough for README Quick Start step 3; `guard-demo.*`.
- `action-demo/` — GitHub Action / CI-focused demo; `action-demo.*`.
